# SDMBCv2 Documentation

Start here:

- `WORKFLOW_INDEX.md`: current operational entry points and where outputs should live
- `runbooks/INTERPOLATION_RUNBOOK.md`: interpolation workflow
- `runbooks/BC_RUNBOOK.md`: bias-correction workflow
- `runbooks/MRMBC_BUILD.md`: Fortran `mrmbc` build/import notes

Historical notes are kept in `archive/`.

The `src/` directory should stay focused on runnable code, reusable submit
helpers, PBS templates, and reusable base configs.
